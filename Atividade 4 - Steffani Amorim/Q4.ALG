algoritmo "Q4"
var
   inicioContagem, finalContagem: inteiro
inicio
   escreva("Início da contagem:")
   leia(inicioContagem)

   escreva("Final da contagem: ")
   leia(finalContagem)

   enquanto (inicioContagem <= finalContagem) faca
      escreval(inicioContagem)
      inicioContagem <- inicioContagem + 1
   fimenquanto

fimalgoritmo